import { storiesOf } from '@storybook/react'
import React from 'react'
import SectionHeading from './SectionHeading'

storiesOf('SectionHeading', module).add('Default', () => (
  <SectionHeading>Section Heading</SectionHeading>
))
